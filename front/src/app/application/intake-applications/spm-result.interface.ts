import {MetaObject} from "../../core/meta-object.interface";
export interface SpmResult extends MetaObject {
  
  year: number;
  grade: string;
 // addMath :String;
 // physic:String;
 // chemist:String;
 // bio:String;
  name:String;
  aggregate:number;
}
