import {MetaObject} from "../../../core/meta-object.interface";
import {StudyMode} from "../common/study-mode.interface";
export interface StudyModeOffering extends MetaObject {
  studyMode:StudyMode;
}
